const fn = require("./X.template");

describe("template", () => {
  it("test 1", () => {
    expect(fn()).toEqual(true);
  });

  it("test 2", () => {
    expect(fn()).toEqual(true);
  });

  it("test 3", () => {
    expect(fn()).toEqual(true);
  });

  it("test 4", () => {
    expect(fn()).toEqual(true);
  });

  it("test 5", () => {
    expect(fn()).toEqual(true);
  });
});
