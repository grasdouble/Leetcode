const template = require('./X.template');

describe('template', () => {
    it('test 1', () => {
        expect(template()).toEqual(true);
    });

    it('test 2', () => {
        expect(template()).toEqual(true);
    });

    it('test 3', () => {
        expect(template()).toEqual(true);
    });

    it('test 4', () => {
        expect(template()).toEqual(true);
    });

    it('test 5', () => {
        expect(template()).toEqual(true);
    });
});